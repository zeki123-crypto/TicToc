# --- Telegram bot framework (modern, fully async) ---
aiogram==3.13.1

# --- Configuration & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Video downloading ---
# TikTok changes its site often; keep yt-dlp recent. Bump this when extraction
# starts failing ("Unable to extract ..."), since fixes ship in new releases.
yt-dlp==2026.6.9

# --- Bundled ffmpeg binary, used when no system ffmpeg is present (e.g. on
#     PaaS auto-build environments). A system ffmpeg on PATH takes priority. ---
imageio-ffmpeg==0.6.0

# --- Watermark rendering. The bundled ffmpeg lacks the drawtext filter, so we
#     render the watermark text to a PNG with Pillow and overlay it instead. ---
pillow==11.0.0

# --- Storage / FSM backend ---
redis==5.1.1

# --- Structured logging ---
structlog==24.4.0

# --- Async HTTP (used by aiogram, pinned for reproducibility) ---
aiohttp==3.10.10
